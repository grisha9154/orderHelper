{
  "name": "with-ts",
  "version": "1.0.0",
  "main": "./dist/app.js",
  "scripts": {
    "start": "cd backend && npm run build && npm run start",
    "start-dev": "cd backend && npm run start"
  }
}
